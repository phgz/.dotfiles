local utils = require('utils')

utils.map('o', 'K', '<Plug>Commentary', {noremap = false})
utils.map('x', 'K', '<Plug>Commentary', {noremap = false})
utils.map('n', 'K', '<Plug>Commentary', {noremap = false})
