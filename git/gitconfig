[user]
    email = <<EMAIL>>
    name = Philip Gaudreau

[credential]
    helper = store

[filter "currentThemeFilter"]
    clean = sed "s/^night$/day/"

[filter "kittyThemeFilter"]
    clean = sed "s/^#\\ Theme-night$/#\\ Theme-day/"
